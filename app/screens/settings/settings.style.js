import { StyleSheet } from "react-native";

export const settingsStyle = StyleSheet.create({
    scrollView: {
        flex: 1
    },
    bodyView: {
        backgroundColor: '#000'
    },
    exampleText: {
        color: '#fff'
    },
})